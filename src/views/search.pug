extends base
include mixins/video

block content 
  form.form__post(method="get") 
    input.form__post-space(type="text" placeholder="Search by Title" name="keyword")
    input(type="submit" value="Search")
  div.video-grid
    each video in videos 
      +video(video)
    else
      span.empty__message No Videos found.